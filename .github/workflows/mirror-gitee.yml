name: Mirror to Gitee
on:
  push:
    branches: [main]
jobs:
  mirror:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - run: |
          git push --mirror https://oauth2:${{ secrets.GITEE_MIRROR_TOKEN }}@gitee.com/no-memes/no-memes.git
